docker volume create

docker volume ls

docker volume create --name gaurav-app-volume --opt type=none --opt device=/PATH/ --opt o=bind

docker run -d --mount source=gaurav-app-volume,traget=/data -p 8000:8000 gaurav.app:latest



# to create volume
# to list the volume
# to create volume and give name to the volume , path , and bind the changes to local to main
# mount with the container
